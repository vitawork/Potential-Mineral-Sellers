
const controller = require('../controllers/index');



module.exports = (app, upload) => {
  /////////////////////////////// GENERAL //////////////////////////////

  // app.get('/api/countries', controller.auth.getCountryFromAPI);

  // app.post('/api/states', controller.auth.getStateFromAPI);

  // /////////////////////////////// GENERAL /////////////////////////////

  // /////////////////////////////// User ///////////////////////////////
  // app.post('/api/register', controller.auth.register);

  // app.post('/api/addUser', controller.auth.addUser);

  // app.get('/api/users', controller.auth.getAllUsers);

  // app.get('/api/user/:email',  controller.auth.getUser);

  // app.get('/api/personId/:id',  controller.auth.getPersonById);

  // app.get('/api/workers',  controller.auth.getAllWorkers);

  // app.put('/api/user/:id', controller.auth.updateUser);

  // app.put('/api/deleteUser/:id', controller.auth.deleteUser);

  /////////////////////////////// User ////////////////////////////////

};
