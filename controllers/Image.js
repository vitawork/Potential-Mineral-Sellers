const db = require ('../models/index');

require ('dotenv').config ();

module.exports = {
  ///////////// Auth ///////////////////////
  save: (req, res, next) => {
    const files = req.files
  },
};